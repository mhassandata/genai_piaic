"use strict";
const names = ['Hassan', 'Zahid', 'Asad', 'Eid'];
for (const name of names) {
    console.log(name);
}
for (const name of names) {
    console.log(`This is the name of person ${name}.`);
}
